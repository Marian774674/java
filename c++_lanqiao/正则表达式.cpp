//int dfs()
//{
//    int res = 0;  // 当前层累计的长度（比如这一层是 "xx|xxx"，res 最终会是 3）
//
//    while (k < s.size())  // 只要没读完字符串，就继续
//    {
//        //  情况1：遇到 '(' → 进入子表达式（递归！）
//        if (s[k] == '(')
//        {
//            k++;           // 跳过 '('
//            res += dfs();  // 【关键】递归计算括号里的内容，结果加到 res
//            k++;           // 递归返回时，k 停在 ')'，跳过它
//        }
//
//        //  情况2：遇到 '|' → "或"运算，取最大值
//        else if (s[k] == '|')
//        {
//            k++;  // 跳过 '|'
//            // 【关键】右边的分支也是一个子问题，递归算它，然后和左边取 max
//            res = max(res, dfs());
//        }
//
//        //  情况3：遇到 ')' → 当前层结束，返回上层
//        else if (s[k] == ')')
//            break;  // 跳出 while，return res
//
//        //  情况4：遇到 'x' → 长度+1
//        else
//        {
//            k++;    // 指针后移
//            res++;  // 长度+1
//        }
//    }
//    return res;  // 返回当前层算出的最长长度
//}